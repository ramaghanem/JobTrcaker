import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';

// اختبار بسيط للتأكد إن كل شيء شغال
describe('Simple Test', () => {
  it('should work', () => {
    expect(true).toBe(true);
  });
});